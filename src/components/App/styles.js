import styled from 'styled-components';

export const Container = styled.div`
  width: 100%; // se a tela tiver menos de 500px vai ocupar 100%
  max-width: 500px; // se a tela tiver mais de 500px vai ocupar no máx 500
  margin: 0 auto;
  padding: 0 16px;
`;
